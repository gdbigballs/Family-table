# 家宴点单 v0.1.2

发布日期：2026-07-31

## 本次更新

- 重做首页已选菜品入口：没有选菜时不显示；选菜后显示底部固定操作栏。
- 移动端底部操作栏调整为左侧购物车、右侧“选好了”，紧贴屏幕底部，方便单手操作。
- 已选菜品可从购物车按钮向上展开查看和移除，避免离开当前菜单页。
- 确认订单页新增“立即点菜 / 预约用餐”切换。
  - 立即点菜无需填写日期、时段和人数。
  - 预约用餐保留完整的时间与人数选择。
- 移动端菜单分类栏会在进入菜单后固定在左侧，滚动到菜单末尾也不会消失。
- 当前浏览的菜单分类会随页面滚动自动高亮。
- 后台“关于项目”和项目包版本同步更新为 v0.1.2。
- README 新增桌面端、移动端菜单和移动端确认订单截图。

## 升级说明

本次更新不涉及数据库结构变更。更新代码后，普通 Node.js 部署重启服务即可；Docker 或 NAS 部署请重新构建并启动容器：

```bash
docker compose up -d --build
```
